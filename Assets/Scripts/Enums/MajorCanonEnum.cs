//A Major Canon is one that radically changes the setting to prevent specific large events from happening, such as broken masquerade.
//These canons typically prevent other minor canons (And lots of the generic proposals) from happening.
public enum MajorCanonEnum
{
    VANILLA,
    BROKENMASQ,
    BELLEVERSE,
    RATSNEST
}